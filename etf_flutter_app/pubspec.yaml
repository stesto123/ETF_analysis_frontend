name: etf_flutter_app
description: A Flutter port scaffold of the ETF_analysis_frontend React Native app
publish_to: 'none'
version: 0.0.1
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  shared_preferences: ^2.1.1
  provider: ^6.0.5
  fl_chart: ^0.60.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - ../assets/images/favicon.png
    - ../assets/images/icon.png
